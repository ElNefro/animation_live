<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Animation Controls" Height="300" Width="300"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#f0f0f0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Expander с чекбоксами анимаций -->
        <Expander Grid.Row="0" 
                  Header="Анимации Curl ASCII" 
                  Margin="10" 
                  Padding="8"
                  Background="White"
                  BorderBrush="#cccccc"
                  BorderThickness="1">
            <StackPanel>
                <CheckBox Content="Волна" Margin="0,2"/>
                <CheckBox Content="Спираль" Margin="0,2"/>
                <CheckBox Content="Исчезание" Margin="0,2"/>
                <CheckBox Content="Типографская" Margin="0,2"/>
                <CheckBox Content="Обратный свиток" Margin="0,2"/>
            </StackPanel>
        </Expander>

        <!-- Контейнер для будущего отображения анимации -->
        <Border Grid.Row="1" 
                Margin="10" 
                Background="White" 
                BorderBrush="#cccccc" 
                BorderThickness="1">
            <TextBlock Text="Здесь будет отображаться анимация..." 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Foreground="Gray"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- Кнопка запуска CMD -->
        <Button Grid.Row="2" 
                Content="Открыть CMD" 
                Margin="10" 
                Padding="8"
                Background="#0078d4" 
                Foreground="White"
                BorderThickness="0"
                FontSize="14"
                Cursor="Hand"/>
    </Grid>
</Window>
